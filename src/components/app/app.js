import React, {Component} from 'react';
import Text from '../text';
import './app.css';

export default class App extends Component{
        render(){
            return(
               <div>
                   <Text/>
               </div>
            )
        }
}